## Panda Village
# Führe aus, wenn der Spieler, der diese Function aufruft, mit dem Tag, teleport den Spieler, der diese Function aufruft, an die Koordinate mit der Ausrichtung
execute if entity @s[tag=spawnpoint_panda_village] run teleport @s 1785 65 268 40 0
# Führe aus, wenn der Spieler, der diese Function aufruft, mit dem Tag, springe zurück und nimm -1 mit
execute if entity @s[tag=spawnpoint_panda_village] run return -1

## Dorf 2
# Führe aus, wenn der Spieler, der diese Function aufruft, mit dem Tag, teleport den Spieler, der diese Function aufruft, an die Koordinate mit der Ausrichtung
execute if entity @s[tag=spawnpoint_dorf2] run teleport @s 838 65 129 40 0
# Führe aus, wenn der Spieler, der diese Function aufruft, mit dem Tag, springe zurück und nimm -1 mit
execute if entity @s[tag=spawnpoint_dorf2] run return -1

## Ohne Tag zum Worldspawn im Panda Village
# Teleport den Spieler, der diese Function aufruft, an die Koordinate mit der Ausrichtung
teleport @s 1736 65 261 -90 0